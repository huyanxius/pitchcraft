interface:
  display_name: "图片 PPT 转可编辑"
  short_description: "转换图片幻灯片或修复底图残字，保留既有对象并逐页回装验证"
  default_prompt: "使用 $image-ppt-to-editable，检查这份 PPT 并按现状转换或修复可编辑文字与底图，保留原版式，已有图片拆分不变，另存新文件。"

policy:
  allow_implicit_invocation: true
